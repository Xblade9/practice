export interface Address {
    id: number,
    add1: string,
    add2: string,
    addType: string,
    appname: string,
    dob: string,
    fname: string
    is_add_same: boolean,
    is_add_diff: boolean,
    message?: string
}